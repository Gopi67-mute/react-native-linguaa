Read AGENTS.md first and follow it strictly.

Use the installed Vision Agents skill and Stream skills to connect the Audio Lesson screen to the Vision Agent so the AI teacher joins the same Stream call as the user.

Add Expo API routes to start and stop the agent that proxy to the Vision Agent server. Validate the Clerk session in both routes and authorize the requested Stream call for the authenticated user. The client may send only the lesson identifier and language code; resolve the lesson, language, goals, vocabulary, phrases, and AI teacher prompt server-side from trusted learning data, then build the agent session and Stream call context from that resolved data. Update the Python agent to consume the server-resolved fields.

The agent's Stream identity and token must be created and owned entirely server-side (in the Expo API routes / Vision Agent server) — never derive it from mobile-provided values or the logged-in Clerk user. Grant the agent only the Stream capability needed to publish audio and call goLive() in audio_room; verify against the installed SDK whether that needs an elevated/admin role or a narrower scoped role, and use the minimum that works. Verify the SDK's actual agent creation, token issuance, and cleanup APIs before implementing. Give each agent session a required server-issued session identifier. Make stop operations idempotent and safe for duplicate or concurrent requests: stop only the requested session and use an atomic session/version check so a delayed stop cannot terminate a newer session. Apply bounded timeouts to cleanup requests, and use a server-side TTL or heartbeat-based expiration to reclaim sessions after app termination when unmount cleanup does not run.

Do not expose any secrets in the mobile app. Keep the existing Stream audio flow intact. Show the agent connection status with idle, connecting, connected, and failed states.
